{
  "name": "sort-viewer",
  "version": "1.0.0",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-typescript": "^7.12.7",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx babel src --out-dir dist --copy-files --extensions '.ts'",
    "start": "nodemon --watch src --ext '*' --exec 'npm run dev' "
  },
  "author": "DaisukiTamago",
  "license": "ISC"
}
